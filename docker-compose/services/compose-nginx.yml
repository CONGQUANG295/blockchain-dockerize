services:
  nginx:
    image: nginx
    container_name: proxy
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    volumes:
      - ../proxy_custom_html/:/usr/share/nginx/html/
      - ../proxy/default.conf.template:/etc/nginx/templates/default.conf.template
    ports:
      - 80:80