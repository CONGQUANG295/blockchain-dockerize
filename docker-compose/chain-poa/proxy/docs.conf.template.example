server {
        listen 80;

        server_name ${DOCS_SERVER_NAME} www.${DOCS_SERVER_NAME};

        root /var/www/docs;
        index index.html index.htm index.nginx-debian.html;

        location / {
                try_files $uri $uri/ =404;
        }
}