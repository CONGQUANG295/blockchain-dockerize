services:
  blockscout:
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=poa-proxy
      - traefik.http.routers.${NETWORK_TYPE:-mainnet}-explorer.rule=Host(`${BLOCKSCOUT_BACK_SERVER_NAME}`) || Host(`www.${BLOCKSCOUT_BACK_SERVER_NAME}`)
      - traefik.http.routers.${NETWORK_TYPE:-mainnet}-explorer.entrypoints=websecure
      - traefik.http.routers.${NETWORK_TYPE:-mainnet}-explorer.tls.certresolver=letsencrypt
      - traefik.http.routers.${NETWORK_TYPE:-mainnet}-explorer.service=${NETWORK_TYPE:-mainnet}-explorer
      - traefik.http.routers.${NETWORK_TYPE:-mainnet}-explorer.middlewares=secure-headers@file
      - traefik.http.services.${NETWORK_TYPE:-mainnet}-explorer.loadbalancer.server.port=4000
      - traefik.http.services.${NETWORK_TYPE:-mainnet}-explorer.loadbalancer.passhostheader=true