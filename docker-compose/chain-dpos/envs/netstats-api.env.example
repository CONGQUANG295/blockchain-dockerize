RPC_HOST=rpc.host
WS_SECRET=${WS_SECRET}
# Remote validators: use STATUS_SERVER_NAME from deploy.env (Traefik → netstats-dashboard)
WS_SERVER=wss://${STATUS_SERVER_NAME}
# Local dev (dashboard on same host): WS_SERVER=ws://host.docker.internal:${PORT}