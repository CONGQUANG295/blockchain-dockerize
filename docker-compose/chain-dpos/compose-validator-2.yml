# Additional validator compose — deferred (v2)
#
# Non-seed validators use genesis/reserved-peers.txt (from export-peer-config on seed host).
# Prepare locally: make prepare-new-validator-local SEED_SERVER=... [NODE_ID=validator-N]
# Filename / service name should match NODE_ID (e.g. compose-validator-3.yml).

name: dpos-new-validator

# include:
#   - path:
#       - ../services/compose-openethereum.yml
#       - ./overrides/<NODE_ID>.override.yml
